<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@android:color/darker_gray"> <!-- Ripple color -->

    <!-- Background color -->
    <item android:drawable="@android:color/white"/>
</ripple>